# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

suite: PageBroker sidecar
templates:
  - templates/daemonset.yaml
  - templates/configmap.yaml
tests:
  - it: runs the PageBroker sidecar next to the agent at the agent's tag
    template: templates/daemonset.yaml
    set:
      image.agent.tag: v1.2.3
    asserts:
      - equal:
          path: spec.template.spec.containers[0].name
          value: agent
      - equal:
          path: spec.template.spec.containers[0].image
          value: ghcr.io/ai-dynamo/snapshot/agent:v1.2.3
      - equal:
          path: spec.template.spec.containers[1].name
          value: pagebroker
      - equal:
          path: spec.template.spec.containers[1].image
          value: ghcr.io/ai-dynamo/snapshot/pagebroker:v1.2.3
      - notExists:
          path: spec.template.spec.containers[1].command

  - it: points the daemon at the shared socket, staging root, and checkpoint PVC path
    template: templates/daemonset.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[1].args
          value:
            - /pagebroker/control/pagebroker.sock
            - /pagebroker/staging
            - /checkpoints
            - --max-concurrent-requests
            - "16"

  - it: mounts the checkpoint PVC in both containers at the same path
    template: templates/daemonset.yaml
    asserts:
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: checkpoints
            mountPath: /checkpoints
      - contains:
          path: spec.template.spec.containers[1].volumeMounts
          content:
            name: checkpoints
            mountPath: /checkpoints
      - contains:
          path: spec.template.spec.volumes
          content:
            name: checkpoints
            persistentVolumeClaim:
              claimName: snapshot-pvc

  - it: shares one memory-backed volume between the agent and the sidecar
    template: templates/daemonset.yaml
    asserts:
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: pagebroker
            mountPath: /pagebroker
      - contains:
          path: spec.template.spec.containers[1].volumeMounts
          content:
            name: pagebroker
            mountPath: /pagebroker
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pagebroker
            emptyDir:
              medium: Memory
              sizeLimit: "64Gi"

  - it: caps the staging volume at the agent memory limit by default
    template: templates/daemonset.yaml
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pagebroker
            emptyDir:
              medium: Memory
              sizeLimit: "64Gi"
      - equal:
          path: spec.template.spec.containers[0].resources.limits.memory
          value: 64Gi

  - it: honours a pageBroker.staging.sizeLimit override
    template: templates/daemonset.yaml
    set:
      pageBroker.staging.sizeLimit: 128Gi
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pagebroker
            emptyDir:
              medium: Memory
              sizeLimit: "128Gi"

  - it: renders when reused values carry no pageBroker.staging map
    template: templates/daemonset.yaml
    set:
      pageBroker.staging: null
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pagebroker
            emptyDir:
              medium: Memory

  - it: requests only the idle daemon footprint
    template: templates/daemonset.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[1].resources.requests
          value:
            cpu: 1
            memory: 2Gi

  - it: follows image.pageBroker overrides
    template: templates/daemonset.yaml
    set:
      image.agent.tag: v1.2.3
      image.pageBroker.repository: registry.internal/snapshot/pagebroker
      image.pageBroker.pullPolicy: IfNotPresent
    asserts:
      - equal:
          path: spec.template.spec.containers[1].image
          value: registry.internal/snapshot/pagebroker:v1.2.3
      - equal:
          path: spec.template.spec.containers[1].imagePullPolicy
          value: IfNotPresent

  - it: tells the agent where the control socket is
    template: templates/configmap.yaml
    asserts:
      - matchRegex:
          path: data["config.yaml"]
          pattern: "pageBroker:\n  controlSocketPath: \"/pagebroker/control/pagebroker.sock\""
